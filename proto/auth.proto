syntax = "proto3";
package auth;

// Setup API
message LoginRequest {
  string email = 1;
  string password = 2;
}
message LoginResponse {
  bool status = 1;
}

service Auth {
  rpc Login(LoginRequest) returns (LoginResponse);

}
